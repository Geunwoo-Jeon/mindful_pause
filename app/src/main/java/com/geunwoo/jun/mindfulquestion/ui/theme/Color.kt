package com.geunwoo.jun.mindfulquestion.ui.theme

import androidx.compose.ui.graphics.Color

// Planit 스타일의 부드러운 파스텔 그린 톤 색상 팔레트
// Primary - 파스텔 그린 톤
val GreenPrimary = Color(0xFF7BC393)        // 부드러운 파스텔 그린
val GreenLight = Color(0xFFA8D5BA)          // 연한 민트 그린
val GreenDark = Color(0xFF5BA876)           // 차분한 그린

// 배경 색상
val BackgroundLight = Color(0xFFFAFAFA)     // 아주 밝은 회색 배경
val CardBackground = Color(0xFFFFFFFF)      // 순백색 카드
val CardBackgroundTint = Color(0xFFF5F5F5)  // 아주 연한 회색

// 텍스트 색상 (가독성 향상)
val TextPrimary = Color(0xFF2D2D2D)         // 부드러운 검은색
val TextSecondary = Color(0xFF8A8A8A)       // 부드러운 회색
val TextTertiary = Color(0xFFC4C4C4)        // 연한 회색

// 목표 카드 색상 (통일된 파스텔 그린 톤)
val GoalAccent = Color(0xFF7BC393)          // 모든 목표에 동일한 파스텔 그린

// 버튼 색상
val ButtonBackground = Color(0xFFF8F8F8)    // 아주 연한 회색 버튼
val ButtonText = Color(0xFF8A8A8A)          // 부드러운 버튼 텍스트

// 테두리 색상
val BorderLight = Color(0xFFE0E0E0)         // 연한 테두리